# 개발 도구 설정 — 사람마다 다르므로 공유하지 않는다
.idea/
*.iml
.vscode/

# Java / Gradle 빌드 산출물
backend/build/
backend/.gradle/

# Node 의존성 — package-lock.json으로 다시 설치한다
node_modules/

# Python 가상환경·캐시
.venv/
__pycache__/

# 환경변수 — OpenRouter API 키가 들어 있다. 절대 커밋하지 않는다
.env
*.env
!.env.example

# 운영체제가 만드는 파일
Thumbs.db
.DS_Store
