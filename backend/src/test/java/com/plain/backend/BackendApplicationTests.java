// =========================================================================================
// [1. 패키지 선언]
// =========================================================================================
package com.plain.backend;

// =========================================================================================
// [2. 수입(Import) 구역]
// =========================================================================================
import org.junit.jupiter.api.Test; // 이 함수가 테스트용이라는 걸 컴퓨터에게 알리는 꼬리표입니다.
import org.springframework.boot.test.context.SpringBootTest; // 가짜로 실제 서버를 완벽하게 모방해서 띄워주는 가동기입니다.

// =========================================================================================
// [3. 본격적인 코드 시작: 건강 검진 센터(Test)]
// 우리가 짠 코드들이 실제로 유저에게 배포되기 전에, 에러 없이 잘 돌아가는지 실험해보는 실험실입니다.
// =========================================================================================

// @SpringBootTest: 
// "스프링아, 가짜(가상)로 실제 우리 서버 환경을 100% 똑같이 띄워서 테스트 환경을 구축해 줘!" 라고 
// 지시하는 엄청난 꼬리표입니다. 이 명찰이 있으면 테스트를 돌릴 때 진짜 서버를 켤 때와 똑같이 모든 부품들이 제대로 조립되는지 꼼꼼하게 검사합니다.
@SpringBootTest
class BackendApplicationTests {

    // @Test:
    // "이 밑에 있는 함수는 실제로 서비스되는 진짜 코드가 아니라, 
    // 부품들이 잘 돌아가는지 실험해보는 테스트 전용 함수니까 따로 분리해서 실행해" 라고 컴퓨터에게 알려줍니다.
    @Test
    // void contextLoads(): '설정(context)들을 무사히 불러오는가(Loads)?' 를 테스트하는 함수입니다.
    void contextLoads() {
        
        // 이 중괄호 { } 안이 텅 비어있는 이유가 궁금하실 겁니다!
        // 코드가 한 줄도 없는데 무슨 테스트를 하냐고요?
        // 이 테스트의 유일한 목적은 '최소한 아무런 설정 에러나 오타 없이, 부품들이 꼬이지 않고 
        // 백엔드 서버 심장이 정상적으로 뛰면서 켜지는가?'를 증명하는 것입니다. 
        // 코드가 텅 비어있어도, 서버 켜기 과정 자체에서 에러가 나면 빨간불(테스트 실패)이 뜨고, 
        // 무사히 켜지면 초록불(테스트 성공)이 뜨면서 "아, 적어도 뼈대는 튼튼하구나!" 하고 안심할 수 있게 해줍니다.
    }
}
